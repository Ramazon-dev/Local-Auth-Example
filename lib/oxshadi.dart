import 'package:flutter/material.dart';

class OxshadiPage extends StatelessWidget {
  const OxshadiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Oxshadi"),
      ),
      body: const Center(
        child: Text(
          "Malades",
        ),
      ),
    );
  }
}
